import { Game } from '../game';
import { ControllerState } from './controllerState';

export class StartScreenController implements ControllerState {
  proc(input: string, game: Game): void {
    throw new Error('Method not implemented.');
  }
}
